[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=scheduler
    --cov=timer
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    -v

